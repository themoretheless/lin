[package]
name = "lin-derive"
version = "0.2.0"
edition = "2024"
rust-version = "1.88"
description = "#[derive(LinRow)] for the Lin local database"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
